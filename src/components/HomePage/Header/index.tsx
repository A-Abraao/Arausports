import styled from 'styled-components'

const HeaderComponent = styled.header`

`

function Header() {
    return (
        <HeaderComponent>
            
        </HeaderComponent>
    )
}

export default Header